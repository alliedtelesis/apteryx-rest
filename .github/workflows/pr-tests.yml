name: Run unit tests

on:
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - autogen:
      run: ./autogen.sh
    - name: configure
      run: ./configure
    - name: make
      run: make
